// export const useRepository = <T>(store) => ({
//   async searchItem(): Promise<T[]> {
//     return $fetch(`/api/${store.$id}/search`, {
//       query: { filter: store.filter, ...store.pagination },
//     });
//   },
// });
